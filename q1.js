function swapValues(x, y) {
    // Swap values using arithmetic operations
    newy = x;
    newx = y;
    return [newx, newy]; // Return the swapped values as an array
}

let result = swapValues(5, 10);
console.log("swapped values of x and y are " + result + " respectively");
